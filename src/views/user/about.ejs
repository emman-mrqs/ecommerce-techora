<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>About — Techora</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet"/>

  <!-- Page styles -->
  <link rel="stylesheet" href="/css/about.css" />
</head>
<body>
  <!-- Header -->
  <nav>
    <%- include("../partials/header.ejs") %>
  </nav>

  <% // Use whatever the controller provides: aboutContent OR about. Fall back to {}. %>
  <% const A = (typeof aboutContent !== 'undefined' && aboutContent) || (typeof about !== 'undefined' && about) || {}; %>

  <!-- Hero -->
  <header class="about-hero">
    <div class="container position-relative">
      <div class="row align-items-center g-4">
        <div class="col-lg-7">
          <h1 class="display-5 fw-bold mb-3 reveal"><%= A.hero_title || "Built for Gadget Lovers." %></h1>
          <p class="lead text-muted mb-4 reveal" style="--d:0.06s">
            <%= A.hero_description || "Techora is a modern gadgets marketplace focused on quality, value, and a seamless shopping experience. From smartphones and audio gear to accessories, we curate only the best—so you don’t have to scroll forever." %>
          </p>
          <div class="d-flex gap-2 reveal" style="--d:.12s">
            <a href="<%= A.cta_primary_href || "#become-seller" %>" class="btn btn-outline-dark px-4 py-2">
              <i class="fa-solid fa-store me-2"></i><%= A.cta_primary_text || "Become a Seller" %>
            </a>
          </div>
        </div>
      </div>
    </div>
  </header>

  <!-- Stats (DO NOT EDIT FROM CMS) -->
  <section class="stats-strip">
    <div class="container">
      <div class="row text-center g-4">
        <div class="col-6 col-md-3">
          <div class="stat-card reveal">
            <div class="stat-num" data-count="<%= (stats && stats.totalProducts) || 0 %>">
              <%= (stats && stats.totalProducts) || 0 %>
            </div>
            <div class="stat-label">Products Created</div>
          </div>
        </div>
        <div class="col-6 col-md-3">
          <div class="stat-card reveal" style="--d:.04s">
            <div class="stat-num" data-count="<%= (stats && stats.totalDelivered) || 0 %>">
              <%= (stats && stats.totalDelivered) || 0 %>
            </div>
            <div class="stat-label">Orders Delivered</div>
          </div>
        </div>
        <div class="col-6 col-md-3">
          <div class="stat-card reveal" style="--d:.08s">
            <div class="stat-num" data-count="<%= (stats && stats.avgRating) || 0 %>">
              <%= (stats && stats.avgRating) || 0 %>
            </div>
            <div class="stat-label">Avg. Rating</div>
          </div>
        </div>
        <div class="col-6 col-md-3">
          <div class="stat-card reveal" style="--d:.12s">
            <div class="stat-num" data-count="<%= (stats && stats.totalSellers) || 0 %>">
              <%= (stats && stats.totalSellers) || 0 %>
            </div>
            <div class="stat-label">Trusted Sellers</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Story -->
  <section class="story">
    <div class="container">
      <div class="row g-4 align-items-center">
        <div class="col-lg-6">
          <div class="story-card reveal">
            <h2 class="fw-bold mb-3"><%= A.story_title || "Our Story" %></h2>
            <p class="text-muted mb-3">
              <%= A.story_p1 || "Techora started with a simple idea: finding good gadgets shouldn’t be hard. We hand-pick products, verify quality, and work closely with sellers so you can shop with confidence." %>
            </p>
            <p class="text-muted mb-0">
              <%= A.story_p2 || "We believe in clear specs, fair pricing, and responsive support. That’s why thousands of customers trust Techora for their daily tech—whether for work, play, or creative pursuits." %>
            </p>
          </div>
        </div>
        <div class="col-lg-6">
          <ul class="values list-unstyled m-0">
            <li class="value-item reveal">
              <i class="fa-solid fa-shield-halved"></i>
              <div>
                <h5><%= A.values_1_title || "Quality First" %></h5>
                <p class="m-0 text-muted"><%= A.values_1_text || "Curated gadgets with verified performance and warranty-ready partners." %></p>
              </div>
            </li>
            <li class="value-item reveal" style="--d:.04s">
              <i class="fa-solid fa-truck-fast"></i>
              <div>
                <h5><%= A.values_2_title || "Fast & Reliable" %></h5>
                <p class="m-0 text-muted"><%= A.values_2_text || "Quick fulfillment, careful packaging, and transparent tracking." %></p>
              </div>
            </li>
            <li class="value-item reveal" style="--d:.08s">
              <i class="fa-solid fa-headset"></i>
              <div>
                <h5><%= A.values_3_title || "Human Support" %></h5>
                <p class="m-0 text-muted"><%= A.values_3_text || "Real people ready to help—before and after checkout." %></p>
              </div>
            </li>
            <li class="value-item reveal" style="--d:.12s">
              <i class="fa-solid fa-recycle"></i>
              <div>
                <h5><%= A.values_4_title || "Responsible Choices" %></h5>
                <p class="m-0 text-muted"><%= A.values_4_text || "Promoting durable gear, repairs, and mindful upgrades." %></p>
              </div>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- Why Techora -->
  <section class="why">
    <div class="container">
      <div class="text-center mb-4">
        <h2 class="fw-bold"><%= A.why_title || "Why Shop at Techora?" %></h2>
        <div class="underline mx-auto"></div>
      </div>

      <div class="row g-4">
        <div class="col-md-4">
          <div class="why-card reveal">
            <i class="fa-solid fa-star"></i>
            <h5 class="mt-2 mb-1"><%= A.why_1_title || "Top-tier Curation" %></h5>
            <p class="text-muted m-0"><%= A.why_1_text || "Only standout gadgets make the cut—fewer choices, better choices." %></p>
          </div>
        </div>
        <div class="col-md-4">
          <div class="why-card reveal" style="--d:.04s">
            <i class="fa-solid fa-tags"></i>
            <h5 class="mt-2 mb-1"><%= A.why_2_title || "Honest Pricing" %></h5>
            <p class="text-muted m-0"><%= A.why_2_text || "Transparent deals with zero dark patterns or surprise fees." %></p>
          </div>
        </div>
        <div class="col-md-4">
          <div class="why-card reveal" style="--d:.08s">
            <i class="fa-solid fa-shield"></i>
            <h5 class="mt-2 mb-1"><%= A.why_3_title || "Secure & Private" %></h5>
            <p class="text-muted m-0"><%= A.why_3_text || "Encrypted checkout and privacy-respecting analytics by design." %></p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Become a Seller -->
  <section class="seller" id="become-seller">
    <div class="container">
      <div class="row align-items-center g-4">
        <div class="col-lg-6">
          <h2 class="fw-bold mb-3 reveal"><%= A.seller_title || "Sell with Techora" %></h2>
          <p class="text-muted reveal" style="--d:.04s">
            <%= A.seller_p || "Have great gadgets or accessories? Join our marketplace and reach thousands of shoppers. We provide tools, analytics, and a dedicated team to help you grow." %>
          </p>
          <ul class="steps list-unstyled">
            <li class="reveal"><span>1</span><%= A.step1 || "Create your seller profile" %></li>
            <li class="reveal" style="--d:.04s"><span>2</span><%= A.step2 || "List products & set inventory" %></li>
            <li class="reveal" style="--d:.08s"><span>3</span><%= A.step3 || "Start selling—get paid fast" %></li>
          </ul>
          <div class="d-flex gap-2 reveal" style="--d:.12s">
            <a href="<%= A.cta_primary_href || "/seller-application" %>" class="btn btn-dark px-4 py-2">
              <i class="fa-solid fa-store me-2"></i><%= A.cta_primary_text || "Apply as Seller" %>
            </a>
            <a href="<%= A.cta_secondary_href || "/seller-application" %>" class="btn btn-outline-dark px-4 py-2"><%= A.cta_secondary_text || "Learn More" %></a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="faq">
    <div class="container">
      <div class="text-center mb-4">
        <h2 class="fw-bold"><%= A.faq_title || "FAQ" %></h2>
        <div class="underline mx-auto"></div>
      </div>

      <div class="accordion reveal" id="faqAcc">
        <div class="accordion-item">
          <h2 class="accordion-header">
            <button class="accordion-button" data-bs-toggle="collapse" data-bs-target="#f1">
              <%= A.faq_1_q || "How do I become a Techora seller?" %>
            </button>
          </h2>
          <div id="f1" class="accordion-collapse collapse show" data-bs-parent="#faqAcc">
            <div class="accordion-body">
              <%- A.faq_1_a || 'Click <strong>Apply as Seller</strong> above or go to <code>/seller-application</code>, fill the form, and our team will review your application.' %>
            </div>
          </div>
        </div>

        <div class="accordion-item">
          <h2 class="accordion-header">
            <button class="accordion-button collapsed" data-bs-toggle="collapse" data-bs-target="#f2">
              <%= A.faq_2_q || "What products can I sell?" %>
            </button>
          </h2>
          <div id="f2" class="accordion-collapse collapse" data-bs-parent="#faqAcc">
            <div class="accordion-body">
              <%- A.faq_2_a || 'We focus on gadgets & accessories—phones, audio, peripherals, wearables, and related gear. All listings must pass quality checks.' %>
            </div>
          </div>
        </div>

        <div class="accordion-item">
          <h2 class="accordion-header">
            <button class="accordion-button collapsed" data-bs-toggle="collapse" data-bs-target="#f3">
              <%= A.faq_3_q || "How quickly are orders fulfilled?" %>
            </button>
          </h2>
          <div id="f3" class="accordion-collapse collapse" data-bs-parent="#faqAcc">
            <div class="accordion-body">
               <%- A.faq_3_a || 'Most orders ship within 24–48 hours. Tracking is provided in your account as soon as the courier scans the parcel.' %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer>
    <%- include("../partials/footer.ejs") %>
  </footer>
  
  <script src="/js/about.js"></script>
</body>
</html>
